name: Build

on:
  push:

jobs:
  build-linux-x64:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
    - uses:  actions/upload-artifact@v3.1.0
      with:
        name: linux-x64
        path: bin
